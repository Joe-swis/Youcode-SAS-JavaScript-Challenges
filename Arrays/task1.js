const prompt = require ('prompt-sync')();
let tqille = prompt('Enter your list: ');

let qrrqy = []

for (let i = 0; i <tqille; i++) {

    let vql = prompt('Enter your list: ');

     array[i] = vql;
    
}














function afficherElements(tab)
{
    let i = 0;
    let len = tab.length;
    while(i < len)
    {
        console.log(tab[i]+'\n');
        i++;
    }
}

afficherElements(list);